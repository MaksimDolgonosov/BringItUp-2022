import Slider from "./slider";

export default class MiniSliderP1 extends Slider {
    constructor(page, nextBtn, prevBtn, activeClass) {
        super(page, nextBtn, prevBtn, activeClass);
        
    }
}